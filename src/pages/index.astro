---
import Layout from "../layouts/Layout.astro";
import Intro from "../components/Intro.astro";
import Description from "../components/Description.astro";
import Cartas from "../components/Cartas.astro";
---

<Layout title="ULTRAMARINOS EL CALVO | BORJA GARCÍA | ESTRELLA MICHELÍN ">
  <!--  <div
    id="loader"
    class="fixed inset-0 bg-white flex items-center justify-center z-50 transition-transform duration-700"
  >
    <img src="/logo.webp" alt="Logo" class="logo-animation" />
  </div> -->
    <Intro />
    <Description />
    <Cartas />
</Layout>
<!--
<script src="/src/loader.js"></script>
<style>
  #loader {
    position: fixed;
    inset: 0;
    background: white;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 50;
    transition: transform 1s ease-in-out;
  }

  /* Logo animado (aumenta y disminuye de tamaño) */
  .logo-animation {
    animation: pulse 0.7s ease-in-out 2; /* 2 ciclos de expansión/contracción */
  }

  @keyframes pulse {
    0%,
    100% {
      transform: scale(0.1);
    }
    50% {
      transform: scale(0.2); /* Ajusta el tamaño del crecimiento */
    }
  }

  .hidden {
    transform: translateY(-110%);
  }

  main {
    opacity: 0;
    transition: opacity 0.7s ease-in-out;
  }

  main.visible {
    opacity: 1;
  }
</style>
-->
